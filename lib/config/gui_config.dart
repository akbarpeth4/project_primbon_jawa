import 'package:flutter/material.dart';

class GUIConfig {
  static const windowTitle = "Kalkulator Hari & Pasaran Jawa";
  static const windowSize = Size(450, 350);
  static const fontMain = TextStyle(fontFamily: 'Helvetica', fontSize: 12);
  static const fontTitle = TextStyle(fontFamily: 'Helvetica', fontSize: 14, fontWeight: FontWeight.bold);
  static const bgColor = Color(0xFFF5F5F5);
  static const btnColor = Color(0xFF4CAF50);
  static const btnTextColor = Colors.white;
  static const padX = 10.0;
  static const padY = 5.0;
}
